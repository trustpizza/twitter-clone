<%= turbo_frame_tag id="tweet_#{@tweet.id}" do %>
  <%= button_to 'Delete Like', 
                  tweet_like_path(@tweet, @like, like: {user_id: current_user.id, tweet_id: tweet.id}),
                  method: :delete
                  %>
<% end %>