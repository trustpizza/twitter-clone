<% if @tweet.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this post from being saved:</h2>

    <ul>
    <% @tweet.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: @tweet, url: user_tweets_path(current_user))  do |form| %>
  <%= form.label :body%>
  <%= form.text_field :body %> <br>
  <%= form.submit %>
<% end %>